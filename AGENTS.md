# AGENTS.md — Grok rules for this project

> Generated by **Grok Chat** after scanning the workspace. Edit freely; Grok reloads this on new chats.

## Snapshot
- **Package / name:** (unnamed workspace)
- **Languages:** detect from sources
- **Stacks:** custom
- **Run (suggested):** `see README / package scripts`
- **Test (suggested):** `see project test runner`

## Detected markers
- (no standard markers found)

## Top-level entries
- `.vscode`
- `css`
- `images`
- `index.html`
- `js`
- `README.md`

## How Grok should work here
1. Prefer **read_file / write_file** for source; avoid shell file hacks.
2. Write **complete files** when editing.
3. Match existing style, layout, and naming.
4. Stay inside the workspace; no secrets in code or commits.
5. Tools already **save to disk** — do not ask the user to open dirty tabs.
6. After tools, summarize paths changed and how to verify.

## Efficiency
- Read only what the task needs.
- Ignore `node_modules`, `dist`, `build`, `target`, `.git`, venv folders.
- Prefer project scripts over inventing new toolchains.

## Security
- Never commit `.env` with real values.
- Validate external input; no SQL/command injection.
- Do not disable auth, TLS, or sandboxing for convenience.

## Scripts / commands (detected)
- (none detected)

## Quality
- Keep changes scoped to the request.
- Preserve public APIs unless the user asks to break them.
- Prefer typed APIs where the stack supports them.

See `.xgrok/BEST_PRACTICES.md` and `.xgrok/SECURITY.md`.

## Gemini-focused guidance
- **Purpose:** Provide concise instructions for AI agents working with the keyword `gemini` — e.g., when the user requests 'gemini' in agent customization workflows.
- **When to use:** Use this guidance when the user explicitly asks to focus on `gemini` behavior, prompts, or skill creation. Prefer updating `AGENTS.md` rather than creating duplicative files.
- **What to include:** brief intent, expected inputs, safe limits, and examples of tasks (code generation, doc updates, lightweight scaffolding). Link to project docs for heavy details.
- **Safety / limits:** Avoid including secrets or large proprietary content. If a request needs sensitive input, ask the user to supply it directly outside of agent files.
